# How to use

```bash
uv venv
source .venv/bin/activate
uv sync 
python -m src
```

# Features

- uv
```
uv sync # install packages in our toml
```
- ruff, format / linter
```
ruff format .
```
- mypy, static type checks
```bash
mypy . 
```